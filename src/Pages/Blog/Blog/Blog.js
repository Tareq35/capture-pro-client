import React from 'react';
import useTitle from '../../../Hooks/useTitle';

const Blog = () => {
  
  useTitle("Blog")
  
    return (
      <section>
        <h1>Blogs</h1>
      </section>
    );
};

export default Blog;